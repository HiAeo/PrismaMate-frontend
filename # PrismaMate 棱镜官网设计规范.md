PrismaMate网站设计文档

### 一、设计系统总则

整体风格定义：
- **理念**：专业、理性、克制，用视觉层次表达信息优先级，而非色彩装饰
- **策略**：以黑白灰为主色，辅以低饱和强调色，避免艳丽色彩
- **空间**：大量留白，均衡的栅格系统，帮助用户聚焦重要信息
- **产品气质**：GEO行业的"查博士"——独立、权威、可信、数据驱动

### 二、颜色体系（Color Tokens）

#### 2.1 主色板

| Token | 色值 | 用途 |
|-------|------|------|
| 页面背景 | `#FFFFFF` | 全局背景 |
| 卡片背景 | `#F8F9FA` | 次级容器、表单区 |
| 主文字 | `#1A1A1A` | 标题、正文 |
| 次级文字 | `#6B7280` | 描述、辅助信息 |
| 弱文字 | `#9CA3AF` | 占位符、禁用态 |
| 分割线 | `#E5E7EB` | 卡片边框、表格线 |
| 悬停态 | `#F3F4F6` | 行悬停、按钮悬停 |

#### 2.2 强调色

| Token | 色值 | 用途 |
|-------|------|------|
| 主强调色（品牌蓝） | `#2563EB` | 主按钮、链接、选中态、进度条 |
| 成功绿 | `#059669` | 检测通过、未被篡改 |
| 警示黄 | `#D97706` | 有差异、注意 |
| 错误红 | `#DC2626` | 被篡改、检测失败 |
| 超出覆盖灰 | `#9CA3AF` | 超出覆盖范围 |

#### 2.3 深色模式

支持全站深色模式切换，深色模式下色板反转为暗色调：
- 页面背景：`#0D1117`
- 卡片：`#161B22`
- 主文字：`#E6EDF3`
- 次级文字：`#8B949E`
- 分割线：`#30363D`

### 三、字体体系（Typography）

- **主字体**：`Inter`（无衬线体，现代科技感）
- **中文字体栈**：`Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`
- **等宽字体**：`"JetBrains Mono", "Fira Code", monospace`（验证码、数据展示）

字体大小和行高严格按以下层级，建议将全局字体大小从默认的14px调整为14px(正文)/16px(强调正文)，以提升阅读舒适度：

| 层级 | 字号/行高 | 字重 | 用途 |
|------|----------|------|------|
| H1 | 32px / 40px | Bold (700) | 页面主标题 |
| H2 | 24px / 32px | SemiBold (600) | 区块标题 |
| H3 | 18px / 28px | SemiBold (600) | 卡片标题、表格标题 |
| 正文强调 | 16px / 24px | Medium (500) | 强调正文 |
| 正文 | 14px / 20px | Regular (400) | 正文、表格内容 |
| 辅助文字 | 12px / 16px | Regular (400) | 标签、说明文字、时间戳 |
| 数据展示 | 28px / 36px | Bold (700) | 仪表盘核心数字 |

### 四、间距系统（Spacing Scale）

采用8px为基准倍数的间距体系：

| Token | 值 | 用途 |
|-------|-----|------|
| xs | 4px | 图标与文字间距 |
| sm | 8px | 紧凑元素间距 |
| md | 16px | 标准元素间距、卡片内边距 |
| lg | 24px | 卡片间距、区块间距 |
| xl | 32px | 大区块分隔 |
| 2xl | 48px | 页面顶部/底部留白 |
| 3xl | 64px | 首页 Hero 区域 |

### 五、圆角与阴影

| 元素 | 圆角 | 说明 |
|------|------|------|
| 小按钮 / 标签 | 4px | 紧凑元素 |
| 标准按钮 / 输入框 | 6px | 标准交互元素 |
| 卡片 | 8px | 内容容器 |
| 模态框 | 12px | 浮层 |

阴影仅用于必要的浮层元素（卡片和表格不使用阴影）：
- 模态框：`0px 8px 32px rgba(0,0,0,0.08)`
- 下拉菜单：`0px 4px 16px rgba(0,0,0,0.06)`

### 六、组件风格

#### 6.1 按钮
- 主按钮：品牌蓝背景 `#2563EB`，白色文字，圆角 6px，Hover 时加深 10%
- 次级按钮：白色背景，`#E5E7EB` 边框，Hover 时背景变 `#F3F4F6`
- 危险操作按钮：错误红描边 + 文字，Hover 时红色背景
- 按钮间距：相邻按钮间距 8px

#### 6.2 卡片
- 白色背景，`#E5E7EB` 细边框（1px），圆角 8px
- 内边距统一 24px
- 卡片内标题与内容间距 16px
- 卡片之间间距 24px

#### 6.3 表格
- 表头：`#F8F9FA` 背景，文字 `#6B7280`，字重 Medium，字号 12px
- 数据行：白色背景，底部 `#E5E7EB` 分割线
- 行悬停：`#F3F4F6` 背景
- 表格无外边框，仅靠分割线区分行

#### 6.4 输入框 / 表单
- 白色背景，`#E5E7EB` 边框，圆角 6px
- 聚焦时边框变为 `#2563EB`，外发光 `0 0 0 3px rgba(37, 99, 235, 0.1)`
- 标签与输入框间距 8px
- 表单元素之间间距 16px

#### 6.5 导航栏
- 白色背景，底部 `#E5E7EB` 细线
- 高度 56px
- Logo 在左，导航链接在中，用户头像在右
- 当前页面对应导航项使用品牌蓝 `#2563EB` 文字标识

### 七、首页布局重构（`/`）

参照 Moonshot 开放平台的首页风格，重构首页：

```
┌─────────────────────────────────────────────┐
│  导航栏（Logo + 导航链接 + 用户头像）         │
├─────────────────────────────────────────────┤
│                                             │
│         PrismaMate 棱镜                     │
│     GEO 效果界的“查博士”——                     │
│     让每一份 GEO 交付都有一份                 │
│     双方认可的第三方检测报告                   │
│                                             │
│   [品牌体检]  [GEO效果检测]  [报告验真]       │
│                                             │
├─────────────────────────────────────────────┤
│  三个核心功能卡片（三列，32px间距）             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐     │
│  │ AI 可见度 │ │ GEO 效果 │ │ 报告验真  │     │
│  │ 体检      │ │ 检测     │ │          │     │
│  │          │ │          │ │          │     │
│  │ 输入品牌   │ │ 上传GEO  │ │ 输入12位  │     │
│  │ 关键词    │ │ 数据     │ │ 验证码    │     │
│  └──────────┘ └──────────┘ └──────────┘     │
├─────────────────────────────────────────────┤
│  页脚（版权信息 + 定价/条款/隐私/验证入口）    │
└─────────────────────────────────────────────┘
```

### 八、执行计划

#### 第一步：安装字体
1. 在 `index.html` 中引入 Inter 字体（Google Fonts CDN）
2. 全局 CSS 设置字体栈

#### 第二步：创建全局样式文件
创建 `src/styles/global.css`，定义：
1. CSS 变量（颜色、间距、圆角、字体）
2. 全局排版样式
3. 按钮/卡片/表格/输入框的基础样式
4. 深色模式变量覆盖（使用 `[data-theme="dark"]` 选择器）

#### 第三步：重构 Layout 组件（`src/components/Layout.vue`）
统一所有页面的导航栏和页脚：
- 导航栏：白色背景、56px 高度、底部细线
- 页脚：引用 `AppFooter.vue`

#### 第四步：重构首页（`src/views/Home.vue`）
按照上述布局结构重构首页。

#### 第五步：逐页面应用新样式
按以下顺序：
1. 登录/注册页
2. 定价页、条款页、隐私页
3. 报告验证页
4. 仪表盘首页
5. 品牌体检中心（及新建/模板页）
6. GEO效果检测（及新建/报告页）
7. 报告对比页
8. 管理后台
9. 会员订阅 + 积分中心

### 九、重要约束

1. **不改业务逻辑**：只改 CSS 样式和布局结构，不动 JS/TS 逻辑代码
2. **保持现有路由不变**：不修改 `src/router/index.ts` 中的路由定义
3. **统一使用 CSS 变量**：所有颜色、间距通过 CSS 变量引用，方便后续一键切换主题
4. **兼容深色模式**：使用 `[data-theme="dark"]` 选择器定义深色模式变量

### 十、PrismaMate Logo SVG文档
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 420 90" width="420" height="90">
  <defs>
    <linearGradient id="spectrum" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#2563EB"/>
      <stop offset="33%" stop-color="#7C3AED"/>
      <stop offset="66%" stop-color="#DB2777"/>
      <stop offset="100%" stop-color="#F59E0B"/>
    </linearGradient>
  </defs>
  
  <!-- 三角形图标和光线整体上移并保持等高 -->
  <!-- 棱镜三角形：调整为高约36px，与文字等高 -->
  <polygon points="20,8 38,36 2,36" fill="none" stroke="#1A1A1A" stroke-width="2.5" stroke-linejoin="round"/>
  
  <!-- 入射光线 -->
  <line x1="20" y1="8" x2="10" y2="4" stroke="#1A1A1A" stroke-width="2" stroke-linecap="round"/>
  
  <!-- 折射光谱 -->
  <line x1="30" y1="36" x2="40" y2="46" stroke="url(#spectrum)" stroke-width="2.5" stroke-linecap="round"/>
  <line x1="38" y1="36" x2="52" y2="48" stroke="#2563EB" stroke-width="2.5" stroke-linecap="round"/>
  <line x1="10" y1="36" x2="2" y2="46" stroke="#F59E0B" stroke-width="2.5" stroke-linecap="round"/>
  
  <!-- 品牌文字：与图标同一排，基线对齐 -->
  <text x="60" y="30" font-family="Inter, -apple-system, sans-serif" font-size="26" font-weight="700" fill="#1A1A1A">PrismaMate</text>
  <text x="60" y="46" font-family="Inter, -apple-system, sans-serif" font-size="14" font-weight="500" fill="#9CA3AF" letter-spacing="2">棱镜报告</text>
  
  <!-- 副标题：整体下方细体小字 -->
  <text x="0" y="82" font-family="Inter, -apple-system, sans-serif" font-size="11" font-weight="300" fill="#9CA3AF" letter-spacing="1" text-anchor="middle" x="210">GEO生成式引擎优化第三方监测平台</text>
</svg>