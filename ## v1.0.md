## v1.0.1 备份内容

- `prismamate-frontend_v1.0.1.zip` - 前端完整代码
- `prismamate-backend_v1.0.1.zip` - 后端完整代码

## 回滚操作步骤

### 方案一：完整回滚（替换整个项目）

> **重要说明**：备份文件只包含源代码（约0.3MB），不包含 `node_modules`（约588MB）。恢复后需要重新安装依赖。

#### 前端回滚
```powershell
# 1. 备份当前版本（可选）
cd D:\PrismaMate专用文件夹
Rename-Item prismamate-frontend prismamate-frontend_current
Move-Item backup\prismamate-frontend_src_v1.0.1.zip .

# 2. 解压备份
Expand-Archive -Path prismamate-frontend_src_v1.0.1.zip -DestinationPath prismamate-frontend -Force

# 3. 安装依赖（自动下载 node_modules）
cd prismamate-frontend
npm install

# 4. 重启前端服务
npm run dev
```

#### 后端回滚
```powershell
# 1. 备份当前版本（可选）
cd D:\PrismaMate专用文件夹
Rename-Item prismamate-backend prismamate-backend_current
Move-Item backup\prismamate-backend_v1.0.1.zip .

# 2. 解压备份
Expand-Archive -Path prismamate-backend_v1.0.1.zip -DestinationPath prismamate-backend -Force

# 3. 重启后端服务
cd prismamate-backend
$env:DEEPSEEK_API_KEY = "sk-682d222860a24ef1a66e95b1c51c8362"
$env:PYTHONPATH = "D:\PrismaMate专用文件夹\prismamate-backend"
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

### 方案二：Git 回滚（推荐）

如果你使用 Git 管理代码：
```bash
# 查看备份时的提交
git log --oneline

# 回滚到指定版本
git checkout v1.0.1

# 或创建回滚分支
git checkout -b rollback-v1.0.1 v1.0.1
```

## 验证回滚是否成功

1. 访问 http://localhost:3000 确认前端正常
2. 访问 http://localhost:8000/docs 确认后端正常
3. 测试登录功能：
   - 用户端：`/login`
   - 管理端：`/admin/login`

## 注意事项

1. **数据库不受影响**：备份只包含代码，不包含数据库
2. **环境变量**：需要重新配置 `.env` 文件
3. **依赖更新**：回滚后建议运行 `npm update` / `pip install -r requirements.txt`
4. **清除缓存**：回滚后建议清除 Vite 缓存 `rm -rf node_modules/.vite`

## 紧急回滚联系人

如需紧急回滚，请联系技术支持。

---

*文档生成时间：2026-05-16 09:03*