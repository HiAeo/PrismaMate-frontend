**CodeBuddy，PrismaMate官网首页改版要求：
**1. 页面内容结构**

从上到下按以下区块布局：

**区块一：Hero 区**
- 主标题：`PrismaMate 棱镜报告`
- 副标题：`AI 可见度，一验知真。`
- 下方一行细体小字：`GEO 生成式引擎优化第三方监测平台`
- 两个按钮：`开始检测`（跳转 `/health-check`）和 `了解产品`（跳转到下方核心产品区）
- 背景：纯黑，字体白

**区块二：品牌故事**
- 小标题：`品牌故事`
- 正文（三段）：
  - 2025年，生成式AI彻底改变了用户获取信息的方式。品牌方突然发现，自己的产品是否被AI推荐、在AI中如何被描述，已经成为一个全新的竞争战场。
  - 于是，“生成式引擎优化”（GEO）行业应运而生。数百家服务商涌入，承诺能让品牌在AI搜索结果中获得更多曝光。但一个根本性的问题很快浮出水面：甲方如何验证乙方承诺的效果？GEO 服务商既当运动员又当裁判，交付的报告数据被包装、美化，甲方却无从验证。
  - PrismaMate 的创立，就是为了解决这个问题。从第一行代码起，PrismaMate 就只有一个信念：让AI时代的品牌竞争，建立在真实数据之上。
  - 背景：纯白，字体黑

**区块三：名称由来**
- 小标题：`PrismaMate 的由来`
- 正文：Prisma，源自希腊语，意为“棱镜”。棱镜本身不创造光，它只是让通过它的光被分解成可见的光谱——我们不制造数据，不操控排名，只是让真实的信息通过我们，被拆解、被呈现、被验证。Mate，意为“伙伴、搭档”。合在一起，PrismaMate 棱镜报告，就是“让真实数据穿过棱镜，成为你决策的依据”。
 - 背景：纯黑，字体白

**区块四：使命与定位（两列布局）**
- 左列：`使命`，正文：让 AI 时代的品牌竞争，建立在真实数据之上。
- 右列：`定位`，正文：PrismaMate 棱镜报告 —— GEO 效果第三方独立检测认证平台。PrismaMate 我们永远不做 GEO 优化，我们永远只做 GEO 的镜子。
 - 背景：纯白，字体黑

**区块五：核心产品（三列卡片）**
- 卡片一：`AI 可见度体检`，描述：未做 GEO 推广的品牌，输入关键词即可定期体检，查看在 AI 大模型中的自然表现，并与历史报告自动对比。
- 卡片二：`GEO 效果检测`，描述：甲方上传 GEO 服务商的优化数据，PrismaMate 独立检测并逐项对比，标注“一致/有差异/超出覆盖范围”。
- 卡片三：`报告验真`，描述：任何人输入 12 位验证码，即可验证报告是否原版出具且未被篡改，无需注册。
- 每个卡片底部加一行小字：`覆盖平台：DeepSeek、Kimi、豆包（持续扩展中）`
 - 背景：纯黑，字体白

**区块六：为什么选择 PrismaMate（五行列表）**
- 小标题：`为什么选择 PrismaMate？`
- 列表项：
  1. 绝对独立：不做 GEO 优化，不接优化订单。只做检测，不做运动员。
  2. 数据真实：所有检测结果基于 AI 平台实时查询，非模拟数据。
  3. 不可篡改：每份报告生成 SHA-256 哈希值存证，支持公开验证。
  4. 历史可溯：同模板报告自动关联，可对比历次检测结果。
  5. 三方认可：定位为 GEO 行业的“查博士”——甲方、乙方都认可的中立检测标准。
   - 背景：纯白，字体黑

**区块七：CTA 区**
- 居中文字：`准备好了解你的品牌在AI眼中的真实表现了吗？`
- 按钮：`免费开始体检`（跳转 `/health-check/new`）
 - 背景：纯黑，字体白

**区块八：页脚**
- 复用已有的 `AppFooter.vue` 组件


**2. 页面风格**黑白极简风

### 一、字体体系

**1. 全局字体栈**

在 `index.html` 的 `<head>` 中引入：
- **MiSans VF**（可变字重字体，用于标题和数字）
- **Geist Mono**（等宽字体，用于验证码、数据展示）

CSS 字体栈配置：
```css
body {
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", "PingFang SC", "Microsoft YaHei", sans-serif;
}

code, .code, .mono {
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", "Geist Mono", monospace;
}
```

**2. 字体大小层级**

| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| H1 | 30px | Bold (700) | 页面主标题 |
| H2 | 20px | SemiBold (600) | 区块标题 |
| H3 | 16px | Medium (500) | 卡片标题 |
| 正文 | 14px | Regular (400) | 正文、表格内容 |
| 辅助 | 12px | Regular (400) | 标签、说明、时间戳 |
| 数据 | 28px | Bold (700) | 仪表盘核心数字 |

---

### 二、颜色体系

**1. 浅色模式（默认）**

```css
:root {
  --background: #FFFFFF;
  --foreground: rgb(31 41 55);
  --muted: #9ca3af;
  --border: #E5E7EB;
  --card-bg: #F8F9FA;
  --hover-bg: #f0f0f0;
  --primary: #1677ff;
  --primary-hover: #4096ff;
  --success: #059669;
  --warning: #D97706;
  --danger: #DC2626;
  --overlay: rgba(0, 0, 0, 0.45);
}
```

**2. 暗黑模式**

```css
[data-theme="dark"] {
  --background: #0D1117;
  --foreground: #E6EDF3;
  --muted: #8B949E;
  --border: #30363D;
  --card-bg: #161B22;
  --hover-bg: #1C2128;
  --primary: #4096ff;
  --primary-hover: #69b1ff;
}
```

**3. 强调色用途**

| 颜色 | 用途 |
|------|------|
| `#1677ff`（主蓝） | 主按钮、链接、选中态、进度条 |
| `#059669`（成功绿） | 检测通过、未被篡改 |
| `#D97706`（警示黄） | 有差异、注意 |
| `#DC2626`（错误红） | 被篡改、检测失败 |
| `#9ca3af`（灰） | 占位符、禁用态、次要信息 |

---

### 三、间距与圆角系统

采用 8px 基准倍数体系：

```css
:root {
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
  --radius-xl: 12px;
}
```

---

### 四、组件样式规范

**1. 按钮**
- 主按钮：`#1677ff` 背景，白色文字，圆角 `6px`，Hover 时变为 `#4096ff`
- 次级按钮：白色背景，`#E5E7EB` 边框，Hover 时背景 `#f0f0f0`
- 危险按钮：红色描边 + 文字，Hover 时红色背景
- 按钮高度统一 32px（中号）/ 40px（大号）

**2. 卡片**
- 白色背景，`#E5E7EB` 细边框（1px），圆角 `8px`
- 内边距 24px，卡片间距 24px
- 卡片标题：16px，字重 500，颜色 `rgba(0,0,0,0.85)`

**3. 导航栏**
- 白色背景，底部 `#E5E7EB` 细线
- 高度 56px
- Logo 在左，导航链接在中，用户头像在右
- 当前页面导航项颜色 `#1677ff`
- 导航项悬停时文字颜色平滑过渡

**4. 表格**
- 表头：`#F8F9FA` 背景，文字 `#9ca3af`，字重 500，字号 12px
- 数据行：白色背景，底部 `#E5E7EB` 分割线
- 行悬停：`#f0f0f0` 背景
- 无外边框

**5. 输入框**
- 白色背景，`#E5E7EB` 边框，圆角 6px
- 聚焦时边框 `#1677ff`，外发光 `0 0 0 2px rgba(22, 119, 255, 0.1)`
- 占位符文字颜色 `#9ca3af`

---

### 五、动态交互方式

**1. 颜色过渡**
所有交互元素的颜色变化使用平滑过渡：
```css
transition: color 0.2s ease, background-color 0.2s ease, border-color 0.2s ease;
```

**2. 悬停反馈**
- 导航项：悬停时文字颜色渐变至 `#1677ff`
- 卡片：悬停时轻微上移 2px + 阴影增强
- 表格行：悬停时背景变为 `#f0f0f0`
- 按钮：悬停时背景色加深 10%

**3. 选中态**
- 使用 `#1677ff` 或浅蓝背景标识当前选中项
- 配合左侧色条或底部下划线

**4. 消息提示**
- 成功：绿色背景 + 白色文字
- 错误：红色背景 + 白色文字
- 警告：黄色背景 + 深色文字

---

### 六、执行步骤

**第一步：创建全局样式文件**
创建 `src/styles/global.css`，定义所有 CSS 变量、全局排版、按钮/卡片/表格/输入框基础样式、暗黑模式覆盖。

**第二步：修改 `index.html`**
在 `<head>` 中引入 MiSans VF 和 Geist Mono 字体。

**第三步：在 `main.ts` 中导入全局样式**
```ts
import './styles/global.css'
```

**第四步：逐页面清理旧样式**
按以下顺序检查并修改，确保每个页面使用 CSS 变量而非硬编码颜色：
1. Layout 组件（导航栏 + 页脚）
2. 首页（Home.vue）
3. 登录/注册页
4. 关于页（About.vue）
5. 品牌体检中心页面组
6. GEO 效果检测页面组
7. 报告验证页
8. 用户仪表盘
9. 管理后台

**第五步：验证**
确认所有页面在浅色/暗黑模式下显示正常，交互反馈统一。

---

### 七、重要约束

1. **不改业务逻辑**：只改 CSS 和布局，不动 JS/TS 代码
2. **统一使用 CSS 变量**：所有颜色、间距通过变量引用
3. **兼容暗黑模式**：使用 `[data-theme="dark"]` 选择器
4. **保持路由不变**：不修改 `router/index.ts`
5. **完成后告知**